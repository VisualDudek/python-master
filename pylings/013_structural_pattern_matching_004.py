# implement fn: apply as jump table for given operators

from operator import add, sub, mul, truediv as div


def apply(operator, left, right):
    pass


assert apply("+", 2, 2) == 4
assert apply("**", 2, 2) is None
